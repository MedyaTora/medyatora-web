"use client";

type Props = {
  initialPhoneNumber?: string | null;
  initialPhoneVerified?: boolean;
  initialBalanceUsd?: number;
  initialWelcomeBonusClaimed?: boolean;
};

export default function PhoneVerificationCard(_props: Props) {
  return null;
}